name: Pre-Commit

on:
  pull_request:
    branches:
      - main

jobs:
  call-workflow:
    permissions:
      contents: read
    uses: nexus-module/.github/.github/workflows/reusable-pre-commit.yml@main
