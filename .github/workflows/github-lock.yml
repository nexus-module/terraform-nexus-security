name: "Lock Threads"

on:
  schedule:
    - cron: "00 2 * * *"

jobs:
  call-workflow:
    permissions:
      issues: write
      pull-requests: write
    uses: nexus-module/.github/.github/workflows/github-lock.yml@main
